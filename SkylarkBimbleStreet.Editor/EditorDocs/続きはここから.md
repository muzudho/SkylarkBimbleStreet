# 続きはここから

## 現状

- `SkylarkBimbleStreet.Editor` は追加済み。
- エディターは `SkylarkBimbleStreet/Stages/stage-001.json` から `stage-008.json` を直接読み書きする。
- 起動、ステージ表示、左クリック選択、ドラッグ移動、`PageUp` / `PageDown` でのステージ切り替え、`S` 保存、`R` 再読み込み、追加、削除、アイテム種類切り替えができる。
- 実際にマップエディターでゲーム内オブジェクトの位置調整ができたことを確認済み。
- 操作説明書は `EditorDocs/利用者向け/マップエディター操作説明書.md` に作成済み。
- 開発メモは `EditorDocs/開発/実装メモ.md` に作成済み。

## 確認

- 直近では `dotnet build .\SkylarkBimbleStreet.sln -c Debug --no-restore`、`dotnet build .\SkylarkBimbleStreet.sln -c Release --no-restore`、`dotnet build .\SkylarkBimbleStreet.slnx -c Debug --no-restore` が警告 0、エラー 0 で成功している。
- エディターの短時間起動確認では JSON 読み込みで落ちないことを確認済み。

## 次にやる候補

- 保存時にバックアップを作る。
- グリッドスナップを足す。
- 矩形の追加、削除、サイズ変更を足す。
- 障害物の移動範囲を編集できるようにする。
- 追加するオブジェクト種類を画面内で選べるようにする。
