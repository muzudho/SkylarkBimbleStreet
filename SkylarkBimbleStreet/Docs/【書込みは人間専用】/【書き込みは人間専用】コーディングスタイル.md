# 【書き込みは人間専用】コーディングスタイル

## アーリーリターンの書き方

ソースコードのコーディングスタイルなんだが、アーリーリターンは、return 文だけの１ステートメントのときは、以下のようにしてくれだぜ（＾▽＾）  

```csharp
// こうではない。
if (isReturn) {
	return;
}

// こう。
if (isReturn) return;
```

